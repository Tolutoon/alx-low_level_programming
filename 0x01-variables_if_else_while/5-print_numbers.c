#include<stdio.h>
/**
 * main - Prints all the numbers of base ten starting from zero
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;

	for(n = 0; n < 10; n++)
	{
		printf('%d',n);
	}
	printf('\n');
	return (0);
}
